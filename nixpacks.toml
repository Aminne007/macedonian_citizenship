[phases.setup]
nixPkgs = ["nodejs", "gcc", "gnumake", "python3"]

[phases.install]
cmd = "npm install"

[phases.build]
cmds = ["npm run build"]

[phases.deploy]
cmd = "NODE_ENV=production node server/index.js"
